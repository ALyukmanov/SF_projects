# Зависимости проекта. Установка: pip install -r requirements.txt && pip install -e .
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pyarrow>=12.0
pyyaml>=6.0
matplotlib>=3.7
seaborn>=0.12

# dev / notebooks
pytest>=7.0
jupyter>=1.0
nbclient>=0.8
nbformat>=5.9

# модели ALS / BPR — отдельное окружение:
#   pip install "numpy<2" "implicit>=0.7,<0.8"
# implicit быстрее и стабильнее работает с numpy<2, см. README, раздел «Модели»

# сервис рекомендаций (нужен для tests/test_service.py и локального запуска)
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.6
prometheus-client>=0.20
httpx>=0.27
