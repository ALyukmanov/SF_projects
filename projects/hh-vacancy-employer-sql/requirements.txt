# Inferred from the imports actually used in notebooks/hh_vacancy_employer_sql.ipynb.
# Pin ranges are bounded but not exact-pinned; adjust to your local Python (notebook was
# authored against Python 3.12).

pandas>=2.0,<3.0
psycopg2-binary>=2.9,<3.0
plotly>=5.20,<7.0
requests>=2.31,<3.0
beautifulsoup4>=4.12,<5.0

# Optional, only needed if you want a Jupyter environment to open/re-run the notebook:
jupyter>=1.0,<2.0
